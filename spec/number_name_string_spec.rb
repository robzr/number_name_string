require "spec_helper"

describe NumberNameString do
  it "has a version number" do
    expect(NumberNameString::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
